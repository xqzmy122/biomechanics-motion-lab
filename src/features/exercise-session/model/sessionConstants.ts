/** Prep countdown after camera + pose are ready (ms) */
export const SESSION_COUNTDOWN_MS = 10_000

/** Continuous valid pose required before reps / timed posture begin (ms) */
export const SESSION_CALIBRATION_HOLD_MS = 5_000

/** Desk posture timed block (ms) */
export const POSTURE_SESSION_MS = 60_000

/** Sample posture good/bad once per second for summary timeline */
export const POSTURE_SAMPLE_INTERVAL_MS = 1_000
